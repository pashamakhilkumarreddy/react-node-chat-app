import styled from 'styled-components';

export const HeaderWrapper = styled.header.withConfig({
  displayName: 'HeaderWrapper'
})`

  border-bottom: 2px solid #f7f7f7;
  box-shadow: rgba(0, 0, 0, 0.24) 0px 0.2rem 0.25rem;
`